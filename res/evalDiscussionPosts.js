// Evaluate Discussion posts or short writing

// other program idea
	// defualt score 20
	// default comments to 3
	// establish base score for initial post
		// prompt for base score for initial post
		// prompt for num comments
		// prompt for score

	// get number of comments by user
	// get user comments
	// count num comments< comments
		// detect if base post is there
			// if so, then add base score / 2 
		// evaluate base post
			// is length greather than 200 words
				// if less than 200, deduct .1*base score for every 10 words
			// is it supported by text?
				// if not, deduct 1/2*base score

		// deduct 1/n for n number of comments
			// add score floor(1/n * base score) 
		// evaluate quality of comment
			// is length greater than 10 words?
			// are there complete sentences?

// output to array of posts
	// score with explanations for each user to array
	